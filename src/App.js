import { Button, Grid } from "@mui/material";

const App = () => {
  return (
    <Grid container>
      <Button>hello</Button>
      <Button>hello</Button>
      <Button>hello</Button>
      <Button>hello</Button>
    </Grid>
  );
};

export default App;
